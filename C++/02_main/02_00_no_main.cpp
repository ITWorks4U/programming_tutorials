/*
* An application always needs a main function. If you're using multiple source files,
* then a single main() function is required only.
*
* Since this is the single file only for the compiler, no application is going to build,
* because the compiler can't find a main instruction.
*/