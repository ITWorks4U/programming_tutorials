#   time measurment
-   measure a time for a certain code instruction may help you out in some situations

>   **NOTE**:   In C++ you're using an another way to measure a time amount.

### required implementations
-   `chrono` library
-   the namespace chrono can be used from the namespace std
    -   std::chrono::(any data types to use)
-   `high_resolution_clock::now()`
    -   receive the current timestamp since application run